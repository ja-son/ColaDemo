(request, state, logger) => {
  return {
	statusCode: 200,
    'headers': {
        'Cache-Control': 'private',
        'Content-Type': 'application\/json; charset=utf-8',
        'Vary': 'Accept, Accept-Encoding'
    },
    body: '<%- stringify(filename, 'questions_list.ejs') %>',
	_mode: 'text',
  };
}